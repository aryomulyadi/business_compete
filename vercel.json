{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "api/index.py": {
      "maxDuration": 60,
      "excludeFiles": "{tests/**,frontend/**,workers/**,output/**,dist/**,.venv/**}"
    }
  },
  "installCommand": "pip install -r requirements-api.txt"
}
